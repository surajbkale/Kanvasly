"use server";

export async function clearCanvas() {
  // Here you can add your canvas clearing logic
  // For example, clearing the database, removing files, etc.
  await new Promise((resolve) => setTimeout(resolve, 500)); // Simulated delay
}
